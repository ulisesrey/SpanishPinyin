Audio clips go here, one file per Clip. The filename stem becomes the Clip id, so a file
must never be renamed once it has been published. Run `npm run build-corpus` after adding
files to regenerate src/study/corpus.json.
